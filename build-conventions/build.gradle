plugins {
    id 'groovy-gradle-plugin'
}

repositories {
    mavenLocal() // to get the kazuki-core version with Double
    gradlePluginPortal()
}

dependencies {
    implementation libs.kotlin.gradlePlugin
}
